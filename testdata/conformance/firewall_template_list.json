{
  "message": "linode.mcp.v1.FirewallTemplateListResponse",
  "description": "linode_firewall_template_list envelope: count and the full proto FirewallTemplate elements (slug plus the shared FirewallRules ruleset). The tool paginates and has no filter param, so filter is omitted.",
  "input": {
    "count": 1,
    "firewall_templates": [
      {
        "slug": "vpc",
        "rules": {
          "inbound": [
            {
              "action": "ACCEPT",
              "protocol": "TCP",
              "ports": "22",
              "addresses": {
                "ipv4": ["0.0.0.0/0"],
                "ipv6": ["::/0"]
              },
              "label": "allow-ssh",
              "description": "Allow inbound SSH"
            }
          ],
          "inbound_policy": "DROP",
          "outbound": [],
          "outbound_policy": "ACCEPT"
        }
      }
    ]
  },
  "canonical": {
    "count": 1,
    "firewall_templates": [
      {
        "slug": "vpc",
        "rules": {
          "inbound": [
            {
              "action": "ACCEPT",
              "protocol": "TCP",
              "ports": "22",
              "addresses": {
                "ipv4": ["0.0.0.0/0"],
                "ipv6": ["::/0"]
              },
              "label": "allow-ssh",
              "description": "Allow inbound SSH"
            }
          ],
          "inbound_policy": "DROP",
          "outbound": [],
          "outbound_policy": "ACCEPT"
        }
      }
    ]
  }
}
