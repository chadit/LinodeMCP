{
  "message": "linode.mcp.v1.DatabaseCredentials",
  "description": "Managed Database connection credentials shared by the MySQL and PostgreSQL credentials-get tools. The password is exposed by design: these tools exist to return the connection secret, so it is emitted rather than redacted.",
  "input": {
    "username": "linroot",
    "password": "example-connection-secret"
  },
  "canonical": {
    "username": "linroot",
    "password": "example-connection-secret"
  }
}
