{
  "tool": "linode_instance_config_delete",
  "description": "Pins the destroy bypass gate (confirm alone blocked in both languages) and the config-delete DELETE after bypass. linode_id/config_id rejection text diverges (see report).",
  "cases": [
    {
      "name": "confirm alone is blocked by the destroy gate",
      "args": { "linode_id": 123, "config_id": 7, "confirm": true },
      "expect_error": "linode_instance_config_delete is destructive. Either:\n  1. Call with dry_run: true first to preview, then call again with\n     confirm: true, confirmed_dry_run: true\n  2. Call with confirm: true, confirm_bypass_dry_run: true to skip preview\n  3. Use yolo: true (only if profile allows)"
    },
    {
      "name": "deletes the config profile",
      "args": { "linode_id": 123, "config_id": 7, "confirm": true, "confirm_bypass_dry_run": true },
      "api_response": {},
      "expect_request": { "method": "DELETE", "path": "/linode/instances/123/configs/7" }
    },
    {
      "name": "dry_run_preview",
      "args": {
        "linode_id": 123,
        "config_id": 7,
        "dry_run": true
      },
      "api_responses": {
        "GET /linode/instances/123/configs/7": {
          "id": 0,
          "label": ""
        }
      },
      "expect_result": {
        "dry_run": true,
        "tool": "linode_instance_config_delete",
        "would_execute": {
          "method": "DELETE",
          "path": "/linode/instances/123/configs/7"
        },
        "current_state": {
          "id": 0,
          "label": ""
        },
        "dependencies": [],
        "side_effects": [],
        "warnings": []
      }
    }
  ]
}
