{
  "tool": "linode_instance_stats_get",
  "description": "GETs this instance's daily stats. The missing-linode_id rejection text diverges (Go 'linode_id is required' vs Python 'linode_id must be a positive integer'), so only the success request is fixtured (see report).",
  "cases": [
    {
      "name": "gets stats with a plain GET",
      "args": { "linode_id": 123 },
      "api_response": {},
      "expect_request": { "method": "GET", "path": "/linode/instances/123/stats" }
    }
  ]
}
