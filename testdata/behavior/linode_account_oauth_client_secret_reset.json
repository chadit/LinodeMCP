{
  "tool": "linode_account_oauth_client_secret_reset",
  "description": "OAuth client secret reset: missing client_id agrees and the single POST once confirmed. Confirm-gate text diverges (see report). The new secret is returned by design.",
  "cases": [
    {
      "name": "requires client_id",
      "args": {},
      "expect_error": "client_id is required"
    },
    {
      "name": "resets an OAuth client secret",
      "args": { "client_id": "abc123", "confirm": true },
      "api_response": { "secret": "new-secret" },
      "expect_request": { "method": "POST", "path": "/account/oauth-clients/abc123/reset-secret" }
    },
    {
      "name": "requires confirm",
      "args": {"client_id": "abc123"},
      "expect_error": "This resets an OAuth client secret. The new secret is only shown once. Set confirm=true to proceed."
    }
  ]
}
