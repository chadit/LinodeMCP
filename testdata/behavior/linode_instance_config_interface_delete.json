{
  "tool": "linode_instance_config_interface_delete",
  "description": "Pins the shared config_id/interface_id positive-int rejections, the destroy bypass gate, and the config-interface DELETE after bypass. linode_id rejection text diverges (see report).",
  "cases": [
    {
      "name": "rejects non-positive config_id",
      "args": { "linode_id": 123, "config_id": 0 },
      "expect_error": "config_id must be a positive integer"
    },
    {
      "name": "rejects non-positive interface_id",
      "args": { "linode_id": 123, "config_id": 7, "interface_id": 0 },
      "expect_error": "interface_id must be a positive integer"
    },
    {
      "name": "confirm alone is blocked by the destroy gate",
      "args": { "linode_id": 123, "config_id": 7, "interface_id": 9, "confirm": true },
      "expect_error": "linode_instance_config_interface_delete is destructive. Either:\n  1. Call with dry_run: true first to preview, then call again with\n     confirm: true, confirmed_dry_run: true\n  2. Call with confirm: true, confirm_bypass_dry_run: true to skip preview\n  3. Use yolo: true (only if profile allows)"
    },
    {
      "name": "deletes the config interface",
      "args": { "linode_id": 123, "config_id": 7, "interface_id": 9, "confirm": true, "confirm_bypass_dry_run": true },
      "api_response": {},
      "expect_request": { "method": "DELETE", "path": "/linode/instances/123/configs/7/interfaces/9" }
    }
  ]
}
