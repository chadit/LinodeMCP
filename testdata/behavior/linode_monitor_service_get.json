{
  "tool": "linode_monitor_service_get",
  "description": "Pins the service GET. The service_type rejection text diverges (Go slug/required/string messages vs Python single message; Go also rejects uppercase and underscore that Python allows), so only the success capture is fixtured (see report).",
  "cases": [
    {
      "name": "gets the monitor service",
      "args": { "service_type": "dbaas" },
      "api_response": {},
      "expect_request": { "method": "GET", "path": "/monitor/services/dbaas" }
    }
  ]
}
