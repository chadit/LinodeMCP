{
  "tool": "linode_instance_interface_delete",
  "description": "Pins the destroy bypass gate and the instance-interface DELETE after bypass. linode_id/interface_id rejection text diverges (see report).",
  "cases": [
    {
      "name": "confirm alone is blocked by the destroy gate",
      "args": { "linode_id": 123, "interface_id": 9, "confirm": true },
      "expect_error": "linode_instance_interface_delete is destructive. Either:\n  1. Call with dry_run: true first to preview, then call again with\n     confirm: true, confirmed_dry_run: true\n  2. Call with confirm: true, confirm_bypass_dry_run: true to skip preview\n  3. Use yolo: true (only if profile allows)"
    },
    {
      "name": "deletes the interface",
      "args": { "linode_id": 123, "interface_id": 9, "confirm": true, "confirm_bypass_dry_run": true },
      "api_response": {},
      "expect_request": { "method": "DELETE", "path": "/linode/instances/123/interfaces/9" }
    }
  ]
}
