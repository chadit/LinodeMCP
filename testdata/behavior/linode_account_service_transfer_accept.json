{
  "tool": "linode_account_service_transfer_accept",
  "description": "Generated from the contract: the token's presence and path-safety rules are declared, so both languages answer the same sentences. Rejections carry confirm because the gate runs before the rules.",
  "cases": [
    {
      "name": "requires token",
      "args": {
        "confirm": true
      },
      "expect_error": "token is required"
    },
    {
      "name": "rejects a token with path separators",
      "args": {
        "token": "abc/def",
        "confirm": true
      },
      "expect_error": "token must not contain path separators, query separators, or traversal segments"
    },
    {
      "name": "requires confirm before accepting",
      "args": {
        "token": "svc-token-123"
      },
      "expect_error": "This accepts an account service transfer. Set confirm=true to proceed."
    },
    {
      "name": "accepts an account service transfer",
      "args": {
        "token": "svc-token-123",
        "confirm": true
      },
      "api_response": {},
      "expect_request": {
        "method": "POST",
        "path": "/account/service-transfers/svc-token-123/accept"
      }
    },
    {
      "name": "requires confirm",
      "args": {
        "token": "svc-token-123"
      },
      "expect_error": "This accepts an account service transfer. Set confirm=true to proceed."
    },
    {
      "name": "rejects a non-string token",
      "args": {
        "token": 123,
        "confirm": true
      },
      "expect_error": "token is required"
    }
  ]
}
