{
  "tool": "linode_database_mysql_instance_credentials_reset",
  "description": "Instance-id validation (with confirm, both languages agree) and the confirm-gated reset POST.",
  "cases": [
    {
      "name": "rejects non-positive instance_id",
      "args": { "instance_id": 0, "confirm": true },
      "expect_error": "instance_id must be a positive integer"
    },
    {
      "name": "resets mysql credentials",
      "args": { "instance_id": 123, "confirm": true },
      "api_response": {},
      "expect_request": { "method": "POST", "path": "/databases/mysql/instances/123/credentials/reset" }
    },
    {
      "name": "requires confirm",
      "args": { "instance_id": 123 },
      "expect_error": "This resets Managed Database credentials. Set confirm=true to proceed."
    }
  ]
}
