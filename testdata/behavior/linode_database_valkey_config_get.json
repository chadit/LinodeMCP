{
  "tool": "linode_database_valkey_config_get",
  "description": "No local validation on this tool; pins the bare config GET the handler issues.",
  "cases": [
    {
      "name": "gets valkey advanced parameters",
      "args": {},
      "api_response": {
        "valkey": {}
      },
      "expect_request": {
        "method": "GET",
        "path": "/databases/valkey/config"
      }
    }
  ]
}
