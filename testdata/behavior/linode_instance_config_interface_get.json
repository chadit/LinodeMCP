{
  "tool": "linode_instance_config_interface_get",
  "description": "Pins the shared config_id/interface_id positive-int rejections and the config-interface GET. linode_id rejection text diverges (see report).",
  "cases": [
    {
      "name": "rejects non-positive config_id",
      "args": { "linode_id": 123, "config_id": 0 },
      "expect_error": "config_id must be a positive integer"
    },
    {
      "name": "rejects non-positive interface_id",
      "args": { "linode_id": 123, "config_id": 7, "interface_id": 0 },
      "expect_error": "interface_id must be a positive integer"
    },
    {
      "name": "gets a config interface",
      "args": { "linode_id": 123, "config_id": 7, "interface_id": 9 },
      "api_response": {},
      "expect_request": { "method": "GET", "path": "/linode/instances/123/configs/7/interfaces/9" }
    }
  ]
}
