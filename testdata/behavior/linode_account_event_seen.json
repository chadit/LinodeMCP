{
  "tool": "linode_account_event_seen",
  "description": "Generated from the contract: event_id's positive-integer rule is declared, so both languages answer the same sentence. Rejections carry confirm because the gate runs before the rules.",
  "cases": [
    {
      "name": "rejects non-positive event_id",
      "args": {
        "event_id": 0,
        "confirm": true
      },
      "expect_error": "event_id must be a positive integer"
    },
    {
      "name": "marks an account event as seen",
      "args": {
        "event_id": 123,
        "confirm": true
      },
      "api_response": {},
      "expect_request": {
        "method": "POST",
        "path": "/account/events/123/seen"
      }
    },
    {
      "name": "requires confirm",
      "args": {
        "event_id": 123
      },
      "expect_error": "This marks an account event as seen. Set confirm=true to proceed."
    },
    {
      "name": "rejects a non-numeric event_id",
      "args": {
        "event_id": "abc",
        "confirm": true
      },
      "expect_error": "event_id must be a positive integer"
    }
  ]
}
