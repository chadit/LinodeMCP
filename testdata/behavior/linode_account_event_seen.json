{
  "tool": "linode_account_event_seen",
  "description": "Shared non-positive event_id rejection and the seen POST path. Confirm-gate and missing-id text diverge (see report).",
  "cases": [
    {
      "name": "rejects non-positive event_id",
      "args": { "event_id": 0 },
      "expect_error": "event_id must be a positive integer"
    },
    {
      "name": "marks an account event as seen",
      "args": { "event_id": 123, "confirm": true },
      "api_response": {},
      "expect_request": { "method": "POST", "path": "/account/events/123/seen" }
    },
    {
      "name": "requires confirm",
      "args": {"event_id": 123},
      "expect_error": "This marks an account event as seen. Set confirm=true to proceed."
    }
  ]
}
