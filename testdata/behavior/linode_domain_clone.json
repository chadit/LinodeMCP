{
  "tool": "linode_domain_clone",
  "description": "Domain clone requires a positive domain_id and a domain, then POSTs the new domain name to /domains/{id}/clone.",
  "cases": [
    {
      "name": "requires a positive domain_id",
      "args": { "confirm": true },
      "expect_error": "domain_id must be a positive integer"
    },
    {
      "name": "requires domain",
      "args": { "confirm": true, "domain_id": 5 },
      "expect_error": "domain is required"
    },
    {
      "name": "clones the domain",
      "args": { "confirm": true, "domain_id": 5, "domain": "clone.example.com" },
      "api_response": {},
      "expect_request": {
        "method": "POST",
        "path": "/domains/5/clone",
        "body": { "domain": "clone.example.com" }
      }
    },
    {
      "name": "requires confirm",
      "args": {"domain_id": 5, "domain": "clone.example.com"},
      "expect_error": "This clones a DNS domain. Set confirm=true to proceed."
    }
  ]
}
