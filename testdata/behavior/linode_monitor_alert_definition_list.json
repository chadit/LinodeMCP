{
  "tool": "linode_monitor_alert_definition_list",
  "description": "Pins the non-integer page rejection both languages share and the unpaginated alert-definitions list GET.",
  "cases": [
    {
      "name": "rejects non-integer page",
      "args": { "page": "1" },
      "expect_error": "page must be an integer"
    },
    {
      "name": "lists monitor alert definitions",
      "args": {},
      "api_response": { "data": [], "page": 1, "pages": 1, "results": 0 },
      "expect_request": { "method": "GET", "path": "/monitor/alert-definitions" }
    }
  ]
}
