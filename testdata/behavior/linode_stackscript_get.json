{
  "tool": "linode_stackscript_get",
  "description": "StackScript get by id. Missing, non-positive, and decimal-string stackscript_id all refuse with the same text in both languages: neither reader takes a string for a number.",
  "cases": [
    {
      "name": "rejects a non-positive stackscript_id",
      "args": {
        "stackscript_id": 0
      },
      "expect_error": "stackscript_id must be a positive integer"
    },
    {
      "name": "rejects a decimal-string stackscript_id",
      "args": {
        "stackscript_id": "1.5"
      },
      "expect_error": "stackscript_id must be a positive integer"
    },
    {
      "name": "requires stackscript_id",
      "args": {},
      "expect_error": "stackscript_id is required"
    },
    {
      "name": "rejects non-positive stackscript_id",
      "args": {
        "stackscript_id": 0
      },
      "expect_error": "stackscript_id must be a positive integer"
    },
    {
      "name": "gets one StackScript by id",
      "args": {
        "stackscript_id": 123
      },
      "api_response": {
        "id": 123,
        "label": "deploy"
      },
      "expect_request": {
        "method": "GET",
        "path": "/linode/stackscripts/123"
      }
    }
  ]
}
