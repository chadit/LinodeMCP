{
  "tool": "linode_vlan_list",
  "description": "VLAN list issues a single GET to the vlans collection. Pagination-bound rejection messages diverge between languages (see report).",
  "cases": [
    {
      "name": "lists vlans",
      "args": {},
      "api_response": { "data": [], "page": 1, "pages": 1, "results": 0 },
      "expect_request": { "method": "GET", "path": "/networking/vlans" }
    }
  ]
}
