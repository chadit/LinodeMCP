{
  "tool": "linode_firewall_rule_version_list",
  "description": "Firewall rule version list GETs the firewall's history subresource. Both languages reject a non-positive firewall_id with the same text; the missing-id message diverges (see report).",
  "cases": [
    {
      "name": "rejects a non-positive firewall_id",
      "args": { "firewall_id": -1 },
      "expect_error": "firewall_id must be a positive integer"
    },
    {
      "name": "lists firewall rule versions",
      "args": { "firewall_id": 5 },
      "api_response": { "data": [], "page": 1, "pages": 1, "results": 0 },
      "expect_request": { "method": "GET", "path": "/networking/firewalls/5/history" }
    }
  ]
}
