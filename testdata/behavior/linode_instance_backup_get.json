{
  "tool": "linode_instance_backup_get",
  "description": "Requires a positive linode_id then backup_id, then GETs the single backup.",
  "cases": [
    {
      "name": "rejects missing linode_id",
      "args": {},
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects a negative linode_id",
      "args": { "linode_id": -1, "backup_id": 456 },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects missing backup_id",
      "args": { "linode_id": 123 },
      "expect_error": "backup_id must be a positive integer"
    },
    {
      "name": "rejects a negative backup_id",
      "args": { "linode_id": 123, "backup_id": -1 },
      "expect_error": "backup_id must be a positive integer"
    },
    {
      "name": "gets one backup with a plain GET",
      "args": { "linode_id": 123, "backup_id": 456 },
      "api_response": { "id": 456 },
      "expect_request": { "method": "GET", "path": "/linode/instances/123/backups/456" }
    }
  ]
}
