{
  "tool": "linode_managed_credential_create",
  "description": "Credential create (Admin): shared empty-username rejection and the POST body once confirmed. Required-field and confirm-gate text diverge (see report). Placeholder password kept short to stay out of the secret scanner. A successful response must be a decodable JSON object.",
  "cases": [
    {
      "name": "rejects empty username",
      "args": { "label": "db", "password": "pw", "username": "  " },
      "expect_error": "username must be a non-empty string"
    },
    {
      "name": "creates a managed credential",
      "args": { "label": "db", "password": "pw", "confirm": true },
      "api_response": { "id": 3, "label": "db" },
      "expect_request": {
        "method": "POST",
        "path": "/managed/credentials",
        "body": { "label": "db", "password": "pw" }
      }
    },
    {
      "name": "requires confirm",
      "args": {"label": "db", "password": "pw"},
      "expect_error": "This creates a stored Managed credential. Set confirm=true to proceed."
    },
    {
      "name": "rejects a bare-array API response",
      "args": { "label": "db", "password": "pw", "confirm": true },
      "api_response": [],
      "expect_api_error": "Failed to create"
    },
    {
      "name": "rejects a non-object API response",
      "args": { "label": "db", "password": "pw", "confirm": true },
      "api_response": 7,
      "expect_api_error": "Failed to create"
    }
  ]
}
