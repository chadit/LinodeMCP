{
  "tool": "linode_account_payment_get",
  "description": "Account payment get: missing payment_id agrees; success pins the GET path. Invalid-id text diverges (see report).",
  "cases": [
    {
      "name": "requires payment_id",
      "args": {},
      "expect_error": "payment_id is required"
    },
    {
      "name": "gets a payment by id",
      "args": { "payment_id": 123 },
      "api_response": { "id": 123, "usd": "10.00" },
      "expect_request": { "method": "GET", "path": "/account/payments/123" }
    }
  ]
}
