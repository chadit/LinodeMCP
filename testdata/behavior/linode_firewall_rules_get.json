{
  "tool": "linode_firewall_rules_get",
  "description": "Firewall rules get GETs the firewall's rules subresource, and rejects a missing or non-positive firewall_id locally (Python now routes through the strict id helper, matching Go).",
  "cases": [
    {
      "name": "requires firewall_id",
      "args": {},
      "expect_error": "firewall_id is required"
    },
    {
      "name": "rejects a negative firewall_id",
      "args": { "firewall_id": -1 },
      "expect_error": "firewall_id must be a positive integer"
    },
    {
      "name": "gets firewall rules",
      "args": { "firewall_id": 5 },
      "api_response": {},
      "expect_request": { "method": "GET", "path": "/networking/firewalls/5/rules" }
    }
  ]
}
