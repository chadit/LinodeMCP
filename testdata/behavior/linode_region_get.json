{
  "tool": "linode_region_get",
  "description": "Region get by slug. The shared region reader answers all three refusals now, so both languages pin the same text.",
  "cases": [
    {
      "name": "requires region_id",
      "args": {},
      "expect_error": "region_id is required"
    },
    {
      "name": "rejects a non-string region_id",
      "args": {
        "region_id": 123
      },
      "expect_error": "region_id must be a non-empty string"
    },
    {
      "name": "rejects a non-slug region_id",
      "args": {
        "region_id": "US-EAST"
      },
      "expect_error": "region_id must be a lowercase region slug containing only letters, numbers, and hyphens"
    },
    {
      "name": "gets one region by slug",
      "args": {
        "region_id": "us-east"
      },
      "api_response": {
        "id": "us-east",
        "country": "us"
      },
      "expect_request": {
        "method": "GET",
        "path": "/regions/us-east"
      }
    }
  ]
}
