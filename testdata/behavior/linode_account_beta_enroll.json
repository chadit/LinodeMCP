{
  "tool": "linode_account_beta_enroll",
  "description": "Generated from the contract: the id's presence and charset are declared rules, so both languages answer the same sentence. Rejections carry confirm because the gate runs before the rules.",
  "cases": [
    {
      "name": "requires id",
      "args": {
        "confirm": true
      },
      "expect_error": "id is required"
    },
    {
      "name": "rejects a charset-invalid id",
      "args": {
        "id": "bad/id",
        "confirm": true
      },
      "expect_error": "id must contain only letters, numbers, underscores, and hyphens"
    },
    {
      "name": "requires confirm before enrolling",
      "args": {
        "id": "my-beta"
      },
      "expect_error": "This enrolls the account in a beta program. Set confirm=true to proceed."
    },
    {
      "name": "enrolls the account in a beta program",
      "args": {
        "id": "my-beta",
        "confirm": true
      },
      "api_response": {},
      "expect_request": {
        "method": "POST",
        "path": "/account/betas",
        "body": {
          "id": "my-beta"
        }
      }
    },
    {
      "name": "requires confirm",
      "args": {
        "id": "my-beta"
      },
      "expect_error": "This enrolls the account in a beta program. Set confirm=true to proceed."
    }
  ]
}
