{
  "tool": "linode_object_storage_quota_get",
  "description": "Pins the single quota GET by ID. The empty/invalid-id rejection text diverges between languages (see report), so only the success path is pinned.",
  "cases": [
    {
      "name": "gets a quota",
      "args": { "obj_quota_id": "obj-bucket-quota" },
      "api_response": { "quota_id": "obj-bucket-quota" },
      "expect_request": { "method": "GET", "path": "/object-storage/quotas/obj-bucket-quota" }
    }
  ]
}
