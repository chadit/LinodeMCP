{
  "tool": "linode_lke_kubeconfig_get",
  "description": "Returns a kubeconfig secret by design; only the request/validation is compared. Pins the cluster_id rejections and the single GET.",
  "cases": [
    {
      "name": "rejects missing cluster_id",
      "args": {},
      "expect_error": "cluster_id is required"
    },
    {
      "name": "rejects a non-positive cluster_id",
      "args": {
        "cluster_id": 0
      },
      "expect_error": "cluster_id must be a positive integer"
    },
    {
      "name": "gets the kubeconfig",
      "args": {
        "cluster_id": 12345
      },
      "api_response": {},
      "expect_request": {
        "method": "GET",
        "path": "/lke/clusters/12345/kubeconfig"
      }
    }
  ]
}
