{
  "tool": "linode_managed_contact_create",
  "description": "Contact create: shared phone-type rejection and the POST body once confirmed. Confirm-gate and empty-body text diverge (see report).",
  "cases": [
    {
      "name": "rejects non-object phone",
      "args": { "phone": "5551234" },
      "expect_error": "phone must be an object"
    },
    {
      "name": "rejects read-only fields",
      "args": { "id": 5, "name": "Ops", "email": "ops@example.com" },
      "expect_error": "id and updated are read-only and cannot be set when creating a managed contact"
    },
    {
      "name": "creates a managed contact",
      "args": { "name": "Ops", "email": "ops@example.com", "confirm": true },
      "api_response": { "id": 12, "name": "Ops", "email": "ops@example.com" },
      "expect_request": {
        "method": "POST",
        "path": "/managed/contacts",
        "body": { "name": "Ops", "email": "ops@example.com" }
      }
    },
    {
      "name": "requires confirm",
      "args": {"name": "Ops", "email": "ops@example.com"},
      "expect_error": "This creates a managed contact. Set confirm=true to proceed."
    }
  ]
}
