{
  "tool": "linode_nodebalancer_config_rebuild",
  "description": "Pins the rebuild POST method and path (body omitted: Go sends no body, Python sends {}). Rejection paths diverge (see report). A response that is not a JSON object must fail rather than yield a zeroed config; the bare-array shape of that case is withheld because Python still fabricates a success from it (see report).",
  "cases": [
    {
      "name": "posts the rebuild request",
      "args": { "nodebalancer_id": 5, "config_id": 7, "confirm": true },
      "api_response": { "id": 7, "nodebalancer_id": 5, "port": 80, "protocol": "http" },
      "expect_request": { "method": "POST", "path": "/nodebalancers/5/configs/7/rebuild" }
    },
    {
      "name": "requires confirm",
      "args": {"nodebalancer_id": 5, "config_id": 7},
      "expect_error": "This rebuilds a NodeBalancer config. Set confirm=true to proceed."
    },
    {
      "name": "rejects a non-object API response",
      "args": { "nodebalancer_id": 5, "config_id": 7, "confirm": true },
      "api_response": 7,
      "expect_api_error": "Failed to rebuild"
    }
  ]
}
