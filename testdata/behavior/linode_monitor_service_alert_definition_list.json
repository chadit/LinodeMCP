{
  "tool": "linode_monitor_service_alert_definition_list",
  "description": "Pins the service alert-definitions GET. The service_type rejection text diverges (see report), so only the success capture is fixtured.",
  "cases": [
    {
      "name": "lists monitor service alert definitions",
      "args": { "service_type": "dbaas" },
      "api_response": { "data": [], "page": 1, "pages": 1, "results": 0 },
      "expect_request": { "method": "GET", "path": "/monitor/services/dbaas/alert-definitions" }
    }
  ]
}
