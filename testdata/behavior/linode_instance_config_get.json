{
  "tool": "linode_instance_config_get",
  "description": "Pins the config-get GET. linode_id/config_id rejection text diverges between languages (see report), so only the success path is shared.",
  "cases": [
    {
      "name": "requires linode_id",
      "args": {},
      "expect_error": "linode_id is required"
    },
    {
      "name": "rejects a non-positive linode_id",
      "args": {
        "linode_id": 0
      },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "requires config_id",
      "args": {
        "linode_id": 1
      },
      "expect_error": "config_id is required"
    },
    {
      "name": "rejects a non-positive config_id",
      "args": {
        "linode_id": 1,
        "config_id": 0
      },
      "expect_error": "config_id must be a positive integer"
    },
    {
      "name": "gets a config profile",
      "args": {
        "linode_id": 123,
        "config_id": 7
      },
      "api_response": {},
      "expect_request": {
        "method": "GET",
        "path": "/linode/instances/123/configs/7"
      }
    }
  ]
}
