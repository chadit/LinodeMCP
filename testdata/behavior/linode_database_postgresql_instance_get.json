{
  "tool": "linode_database_postgresql_instance_get",
  "description": "Pins the instance GET path plus the Option B instance_id rejections, which now agree across languages.",
  "cases": [
    {
      "name": "gets a postgresql instance by id",
      "args": { "instance_id": 123 },
      "api_response": { "id": 123, "label": "prod-pg" },
      "expect_request": { "method": "GET", "path": "/databases/postgresql/instances/123" }
    },
    {
      "name": "rejects missing instance_id",
      "args": {},
      "expect_error": "instance_id is required"
    },
    {
      "name": "rejects zero instance_id",
      "args": { "instance_id": 0 },
      "expect_error": "instance_id must be a positive integer"
    },
    {
      "name": "rejects non-integer instance_id",
      "args": { "instance_id": "123" },
      "expect_error": "instance_id must be a positive integer"
    }
  ]
}
