{
  "tool": "linode_instance_ip_get",
  "description": "Requires a positive linode_id then address, rejects a malformed or zoned address locally against net.ParseIP's legal set, then GETs the single IP.",
  "cases": [
    {
      "name": "rejects missing linode_id",
      "args": {},
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects a negative linode_id",
      "args": { "linode_id": -1, "address": "203.0.113.5" },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects missing address",
      "args": { "linode_id": 123 },
      "expect_error": "address is required"
    },
    {
      "name": "reads a non-string address as absent",
      "args": { "linode_id": 123, "address": 5 },
      "expect_error": "address is required"
    },
    {
      "name": "rejects a malformed address",
      "args": { "linode_id": 123, "address": "not-an-ip" },
      "expect_error": "address must be a valid IP address"
    },
    {
      "name": "rejects a zoned IPv6 address",
      "args": { "linode_id": 123, "address": "fe80::1%eth0" },
      "expect_error": "address must be a valid IP address"
    },
    {
      "name": "gets one IP with a plain GET",
      "args": { "linode_id": 123, "address": "203.0.113.5" },
      "api_response": { "address": "203.0.113.5" },
      "expect_request": { "method": "GET", "path": "/linode/instances/123/ips/203.0.113.5" }
    }
  ]
}
