{
  "tool": "linode_ipv6_pool_list",
  "description": "IPv6 pool list issues a single GET to the pools collection. Pagination-bound rejection messages diverge between languages (see report).",
  "cases": [
    {
      "name": "lists ipv6 pools",
      "args": {},
      "api_response": { "data": [], "page": 1, "pages": 1, "results": 0 },
      "expect_request": { "method": "GET", "path": "/networking/ipv6/pools" }
    }
  ]
}
