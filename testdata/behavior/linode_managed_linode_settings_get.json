{
  "tool": "linode_managed_linode_settings_get",
  "description": "Managed Linode settings get: shared positive-id rejection and the settings GET.",
  "cases": [
    {
      "name": "requires linode_id",
      "args": {},
      "expect_error": "linode_id is required"
    },
    {
      "name": "rejects a non-positive linode_id",
      "args": {
        "linode_id": 0
      },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects an linode_id past the JSON-safe range",
      "args": {
        "linode_id": 9007199254740992
      },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "rejects non-positive linode_id",
      "args": {
        "linode_id": 0
      },
      "expect_error": "linode_id must be a positive integer"
    },
    {
      "name": "gets managed settings for a linode",
      "args": {
        "linode_id": 12
      },
      "api_response": {
        "id": 12,
        "label": "web"
      },
      "expect_request": {
        "method": "GET",
        "path": "/managed/linode-settings/12"
      }
    }
  ]
}
