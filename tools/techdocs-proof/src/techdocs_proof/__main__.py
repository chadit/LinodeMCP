"""Entry point, so `python -m techdocs_proof` is the one way to run it."""

from __future__ import annotations

from techdocs_proof.proof import main

if __name__ == "__main__":
    raise SystemExit(main())
