"""Tests for the config-interface update client route.

The tool itself is generated from the proto contract; what stays here is the
client method it calls, whose route, body, and no-replay posture no generated
test covers.
"""

from __future__ import annotations

from typing import TypeVar

T = TypeVar("T")
