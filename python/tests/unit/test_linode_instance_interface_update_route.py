"""The client method behind linode_instance_interface_update.

The tool itself is generated, so its arguments and answers are pinned by
``testdata/behavior/linode_instance_interface_update.json``; what is left here
is the route the client puts on the wire.
"""

from __future__ import annotations

from typing import TypeVar

T = TypeVar("T")
