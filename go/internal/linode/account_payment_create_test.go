package linode_test

// temporaryPaymentError outlives this file's removed payment tests because the
// managed, instance, and grants retry suites still assert on it.
const temporaryPaymentError = "temporary"
