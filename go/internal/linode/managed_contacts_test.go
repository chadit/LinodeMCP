package linode_test

// Const stub: the delete-contact tests left with the dead client methods, but
// suites across this package still read this auth-header fixture.
const managedContactAuthHeader = "Bearer my-token"
