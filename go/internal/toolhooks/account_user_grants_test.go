package toolhooks_test

// The grant argument spellings the write hooks read, kept here because the
// families that use them sit in four other files.
const (
	keyGrantLinode = "linode"
	keyGrantID     = "id"
)
