# Accepted (known) deviations between the tool scope mapping and the
# Linode OpenAPI spec's per-operation security blocks. Ratchet: fix
# one and remove its line; never add a line by hand (regenerate
# instead, then attach the required annotation). Regenerate with:
#   python3 scripts/verify_sync_scopes.py --update-baseline
# Every entry MUST carry an annotation naming when it was accepted
# and the tracking issue that will close it:
#   <entry>  # accepted YYYY-MM-DD <tracking-issue URL>
linode_networking_reserved_ip_create: route POST /networking/reserved/ips not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
linode_networking_reserved_ip_delete: route DELETE /networking/reserved/ips/{p} not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
linode_networking_reserved_ip_get: route GET /networking/reserved/ips/{p} not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
linode_networking_reserved_ip_list: route GET /networking/reserved/ips not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
linode_networking_reserved_ip_type_list: route GET /networking/reserved/ips/types not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
linode_networking_reserved_ip_update: route PUT /networking/reserved/ips/{p} not in spec  # accepted 2026-07-21 https://github.com/chadit/LinodeMCP-Issue/issues/1038
